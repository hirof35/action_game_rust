Bevy Rust Action GameRust製のデータ指向ゲームエンジン Bevy と、物理シミュレーションライブラリ Rapier2d を活用した、シンプルな2Dプラットフォーム・アクションゲームです。🎮 ゲームの特徴物理ベースのアクション: bevy_rapier2d を採用し、重力、摩擦、衝突、インパルス（衝撃）によるリアルなジャンプ挙動を実現しています。ECSアーキテクチャ: Bevy独自のエンティティ・コンポーネント・システムに基づいた、モジュール化されたクリーンなコード設計。状態遷移システム: GameState によるタイトル・プレイ・ゲームオーバー・クリア画面のスムーズな遷移。ボス・エンカウント: ザコ敵を3体撃破すると出現するボスバトル。カメラフォロー: プレイヤーの移動に合わせてカメラがX軸方向に自動追従します。🕹 操作方法キーアクション← / →プレイヤーの左右移動Spaceジャンプ / ゲーム開始・リスタート🚀 開発環境のセットアップ前提条件Rust (最新のStable版を推奨)コンパイルと実行リポジトリをクローンし、以下のコマンドを実行してください。Bashcargo run --release
※開発中は --release フラグなしでも実行可能ですが、Bevyのパフォーマンスを最大限に発揮させるためにリリースモードを推奨します。🏗 技術的な解説1. 物理演算と衝突判定プレイヤーと敵の「踏みつけ」判定は、衝突イベント（CollisionEvent）と座標の比較を組み合わせて実装されています。Rustif p_trans.translation.y > e_trans.translation.y + 15.0 {
    // プレイヤーが敵より高い位置にいれば「踏みつけ」成功
    commands.entity(e_ent).despawn_recursive();
}
2. コンポーネント指向設計各エンティティ（Player, Enemy, Boss）は独立したコンポーネントとして定義され、システムがそれらを効率的にクエリして処理します。Player: キーボード入力の受付Enemy: 自動巡回AIロジックHealth: 体力管理と終了条件のトリガー3. 可変ジャンプの実装Spaceキーを離した際に上昇速度を減衰させることで、本家アクションゲームのような「ボタンを押す長さで高さが変わるジャンプ」を再現しています。📂 コード構成GameState: アプリケーションの状態管理。setup_game: ステージ、床、プレイヤー、初期敵の初期化。enemy_ai: 境界値による巡回ロジック。collision_logic: プレイヤー、敵、ボス間の衝突とライフ減少・撃破の処理。📝 ライセンスこのプロジェクトは MITライセンス の下で公開されています。
